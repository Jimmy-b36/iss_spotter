const { fetchMyIp } = require("../iss");
const assert = require("chai");

describe("#iss:", () => {
  it("should return an ip if one is found", (done) => {
    assert.equal(err, null);

    done();
  });
});
